# Bankanalysis_Group7
Bank Transactions - Analysis tasks:
1. Compute total deposits and withdrawals per account and overall.
2. Identify accounts with negative balance if any (starting balance + transactions).
3. Detect outlier transactions (amount > 90th percentile).
4. Visualizations: histogram (transaction amounts), bar chart (top accounts by volume).
